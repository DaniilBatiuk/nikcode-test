/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ["images.unsplash.com", "static8.depositphotos.com"],
  },
};

export default nextConfig;
